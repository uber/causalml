# Not used by the Read the Docs build, which creates the environment from
# docs/environment-py311-rtd.yml (see .readthedocs.yml).  This file is for
# building the docs in a plain virtualenv, and it is the manifest Snyk scans.
#
# Snyk tests the *lowest* version a range allows, so an open floor is read as a
# claim that the oldest release still works.  The floors below mirror
# pyproject.toml and the conda environment instead of being left open.
Cython>=3.0.11
numpy>=1.25.2
scikit-learn>=1.6.0
matplotlib>=3.9.2
sphinx>=8.2,<10
pydata-sphinx-theme>=0.20.0
sphinxcontrib-bibtex
nbsphinx
# nbsphinx renders every notebook code cell as `ipython3`, and that Pygments
# lexer ships with IPython.  8.10.0 clears GHSA-29gw-9793-fvw7.
ipython>=8.10.0
# Sphinx pulls Pygments in regardless; < 2.20.0 carries a ReDoS in AdlLexer
# (GHSA-5239-wwwm-4pmq).
pygments>=2.20.0
